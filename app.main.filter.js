(function() {
    'use strict';

    angular
        .module('myApp')
        .filter('KeywordFilter', KeywordFilter);

    function KeywordFilter() {
        return filterFilter

        function filterFilter(params) {
            return params;
        }
    }
})();
